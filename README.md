BeagleboardCrossCompile
=======================

a demonstration for the cross compilation process in beagleboard

cross compiled using `arm-angstrom-linux-gnueabi-gcc` toolchain

you can run it using `qemu-arm-static ./gsoc.bin`
